# ===========================================
# movapic-neo Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values
# cp .env.sample .env.local

# -------------------------------------------
# Application
# -------------------------------------------
# NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://192.168.110.136:3000

# -------------------------------------------
# Database (PostgreSQL)
# -------------------------------------------
DATABASE_URL=postgresql://user:password@localhost:5432/movapic

# -------------------------------------------
# Authentication
# -------------------------------------------
# JWT secret (at least 32 characters)
# Generate: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key-here-at-least-32-chars

# Token encryption key (64 character hex string = 32 bytes for AES-256-GCM)
# Generate: openssl rand -hex 32
TOKEN_ENCRYPTION_KEY=your-64-character-encryption-key

# -------------------------------------------
# S3互換ストレージ (Image Storage)
# -------------------------------------------
# AWS S3 / MinIO などのS3互換ストレージに対応
# S3_ENDPOINT にエンドポイントURLを設定する
#   - AWS S3: https://s3.<region>.amazonaws.com
#   - その他: 各サービスのエンドポイントURL
S3_ENDPOINT=https://s3.ap-northeast-1.amazonaws.com
S3_REGION=ap-northeast-1
S3_ACCESS_KEY_ID=your-access-key-id
S3_SECRET_ACCESS_KEY=your-secret-access-key
S3_BUCKET_NAME=your-bucket-name
S3_PUBLIC_URL=https://your-public-url.example.com

# -------------------------------------------
# Mastodon Bot (Mention Posting)
# -------------------------------------------
MASTODON_BOT_INSTANCE_URL=https://handon.club
MASTODON_BOT_INSTANCE_DOMAIN=handon.club
MASTODON_BOT_ACCESS_TOKEN=your-bot-access-token
MASTODON_BOT_ACCT=pic

# -------------------------------------------
# 管理者（通報の確認・対応 / Bot通知の宛先）
# -------------------------------------------
# 管理者の acct（username@domain）をカンマ区切りで列挙。/admin/reports の閲覧と
# モデレーション対応APIの権限判定、通報の Bot direct 通知の宛先にそのまま使う。
# 例: ADMIN_ACCTS=alice@handon.club,bob@example.com
ADMIN_ACCTS=

# -------------------------------------------
# Internal API
# -------------------------------------------
# Email posting API key
# Generate: openssl rand -hex 32
INTERNAL_API_KEY=your-internal-api-key

# メール投稿の受信ドメイン（ユーザーの宛先 <emailPrefix>@<EMAIL_DOMAIN>）
# 未設定時は "pic.handon.club" にフォールバック
EMAIL_DOMAIN=pic.handon.club

# -------------------------------------------
# コンポーネント役割（3-tier: web / worker-front / compute）
# -------------------------------------------
# 各 pod の役割を起動時に確定する（同一イメージ・middleware/instrumentation が参照）。
#   web          : ページ＋軽量API。/api/internal/* は 404。consumer 起動しない。
#   worker-front : generate/post/ingest を配信＋キュー consumer を起動。/api/internal/* は 404。
#   compute      : /api/internal/* と /api/health のみ提供（他は 404）。consumer 起動しない。
# 未設定（ローカル）= all-in-one（全ルート＋consumer＋sharp）。
# COMPONENT_ROLE=worker-front
# 同時実行ジョブ数（worker-front の consumer 用）。既定 3。
# WORKER_CONCURRENCY=3

# -------------------------------------------
# compute サービス（画像生成専用・内部呼び出し）
# -------------------------------------------
# worker-front → compute の内部呼び出し先（ClusterIP の Service 名など）。
# COMPUTE_SERVICE_URL=http://movapic-neo-compute
# worker-front ⇄ compute の共有鍵（X-API-Key）。compute pod には本キーのみ渡す。
# Generate: openssl rand -hex 32
COMPUTE_API_KEY=your-compute-api-key

# -------------------------------------------
# 管理ヘルスチェック（/admin/stats）
# -------------------------------------------
# web pod → worker-front の内部呼び出し先（ClusterIP の Service 名など）。
# /admin/stats のコンポーネント稼働状況が worker-front の /api/health/stream を叩いて
# Bot Streaming 等の状態を取得するのに使う。未設定なら該当カードは「確認不可」。
# dev の all-in-one（COMPONENT_ROLE 未設定）では不要（同一プロセスを直接参照）。
# WORKER_FRONT_INTERNAL_URL=http://movapic-neo-worker-front

# -------------------------------------------
# Media Proxy
# -------------------------------------------
MEDIA_PROXY_BASE_URL=https://delivery.piyo.me

# -------------------------------------------
# Optional
# -------------------------------------------
# ログインを許可する Fediverse サーバー（カンマ区切り）。
# 未設定だと「全サーバー許可」になる点に注意。複数サーバーを許可すると
# 別サーバーの同名ユーザーも作成され、その場合プロフィールURLは
# username@domain 形式になる（ホームインスタンスのみ素の username）。
# 単一インスタンス運用なら必ずホームインスタンスのみを設定すること。
ALLOWED_SERVERS=handon.club

# ホームインスタンス（プロフィールURLで domain を省略できる既定サーバー）は
# コード側の定数 DEFAULT_INSTANCE（src/lib/userHandle.ts）で定義する。
# ※ 旧 MASTODON_INSTANCE 変数は廃止（現在どこからも参照されていない）。
#   ホームインスタンスを変える場合は上記定数と ALLOWED_SERVERS を合わせること。
